import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brimstonetech.budgetguard',
  appName: 'BudgetGuard',
  webDir: 'dist'
};

export default config;
